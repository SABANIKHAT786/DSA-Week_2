if(curr.length() == 2 * n) {
            ans.add(curr);
            return;
        }

        if(open < n) {
            solve(curr + "(", open + 1, close, n, ans);
        }

        if(close < open) {
            solve(curr + ")", open, close + 1, n, ans);
        
